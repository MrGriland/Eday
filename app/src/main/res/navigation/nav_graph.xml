<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/navigation_second">
    <fragment
        android:id="@+id/navigation_first"
        android:name="bgv.fit.bstu.eday.FirstFragment"
        android:label="Days"
        tools:layout="@layout/first_fragment" />
    <fragment
        android:id="@+id/navigation_second"
        android:name="bgv.fit.bstu.eday.SecondFragment"
        android:label="Today"
        tools:layout="@layout/second_fragment" />
    <fragment
        android:id="@+id/navigation_third"
        android:name="bgv.fit.bstu.eday.ThirdFragment"
        android:label="Profile"
        tools:layout="@layout/third_fragment" />
</navigation>